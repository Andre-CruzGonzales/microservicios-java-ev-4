server:
  port: 8085
  servlet:
    context-path: /ms-employee
    
  error:
    include-details: always
       
# Registry Client
eureka:
   client:
      fetch-registry: true
      serviceUrl:
         defaultZone: http://localhost:8889/eureka

    